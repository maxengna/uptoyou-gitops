apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

resources:
  - service-account.yaml

# Note: Update the role ARN annotation with the actual value from Terraform output
# Command: terraform output -raw ses_service_account_role_arn
