apiVersion: v1
kind: Service
metadata:
  name: backend-app
  namespace: uptoyoushop
spec:
  selector:
    app: uptoyoushop
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
  type: ClusterIP
