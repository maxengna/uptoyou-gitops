apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: aws-load-balancer-controller
  namespace: flux-system
spec:
  interval: 15m
  chart:
    spec:
      chart: aws-load-balancer-controller
      version: ">=1.7.0"
      sourceRef:
        kind: HelmRepository
        name: eks-charts
        namespace: flux-system
      interval: 1h
  install:
    createNamespace: true
  values:
    clusterName: "uptoyou-eks-cluster"
    region: "ap-southeast-1"
    vpcId: "vpc-xxxxxxxxxxxx" # ID ของ VPC (ดูได้จาก terraform output)
    serviceAccount:
      create: true
      name: aws-load-balancer-controller
      annotations:
        # 🔥 สำคัญ: ใส่ ARN ของ IAM Role ที่สร้างจาก Terraform
        eks.amazonaws.com/role-arn: "arn:aws:iam::[ACCOUNT_ID]:role/uptoyou-eks-cluster-lb-controller-role"
